namespace Handledning;

class Program
{
    static void Main(string[] args)
    {
    }
    
    /// <summary>
    /// Prints in the console the numbers from 1 to n (inclusive) in
    /// reverse order, one for each line
    /// </summary>
    /// <param name="n"></param>
    static void PrintReverse(int n)
    {
        
    }
    
    /// <summary>
    /// Returns a copy of the specified array with the elements in reversed order.
    /// </summary>
    /// <example>
    /// int[] arr = {1, 3, 5, 7};
    /// var reversed = ArrayReverse(arr);
    /// reversed == {7, 5, 3, 1}
    /// </example>
    /// <param name="array"></param>
    static T[] ArrayReverse<T>(T[] array)
    {
        return default;
    }
}