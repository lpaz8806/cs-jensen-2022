namespace L09DataStructures.Exercises;

public static class Excercises
{
    /// <summary>
    /// Given a string containing opening and closing parentheses,
    /// check if it represents a balanced expression or not.
    /// </summary>
    /// <example>
    /// HAPB("(el (queso) es delicioso ())") => true
    /// HAPB("(comer (engorda)") => false
    /// </example>
    public static bool HasAllParenthesisBalanced(string s)
    {
        return false;
    }
    
    /// <summary>
    /// Given a string containing opening and closing braces () [] {},
    /// check if it represents a balanced expression or not.
    /// </summary>
    /// <example>
    /// IBE("2 ^ {3 * [2 ^ (1+2)]}") => true
    /// IBE("2 ^ {3 * [2 ^ (1+2)}") => false
    /// </example>
    public static bool IsBalancedExpression(string s)
    {
        return false;
    }
}